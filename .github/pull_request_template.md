## What / 変更内容

## Why / 理由

## How I verified / 確認したこと

- [ ] `make test` passes / テストが通る
- [ ] Checked in the running app / 実際のアプリで確認した
- [ ] Updated all languages in `Localizable.xcstrings` (if UI text changed) / UI の文言を変えた場合、全言語を更新した
- [ ] Updated all `README.*.md` (if documented behavior changed) / README に書かれた動作を変えた場合、全言語を更新した
